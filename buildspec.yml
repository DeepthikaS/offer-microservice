version: 0.2

phases:
  build:
    commands:
      - echo Entered the build phase...
      - DskipTests clean package
  post_build:
    commands:
      - echo Build completed successfully
artifacts:
  files:
    - target/offer-service.jar
  discard-paths: yes